mongo:
    build: docker/mongo
    ports:
        - "27017:27017"
node:
    build: docker/node
    working_dir: /server
    command: node example.js
    ports:
        - "80:80"
    volumes:
        - .:/server
    links:
        - mongo:db
