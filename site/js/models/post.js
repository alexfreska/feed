define(function(require) {
    var $           = require('jquery'),
        _           = require('underscore'),
        Backbone    = require('backbone');

    Post = Backbone.Model.extend({
        defaults: {


        }
    });

    return Post;

});
